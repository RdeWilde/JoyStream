{
  "name": "joystream-example",
  "version": "0.0.0",
  "description": "",
  "main": "basic.js",
  "scripts": {
    "buyer": "DEBUG=* node buyer.js",
    "seller": "DEBUG=* node seller.js",
    "start": "DEBUG=* node basic.js",
    "concurrent": "concurrently \"DEBUG=seller node seller.js\" \"DEBUG=buyer node buyer.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Me",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^3.3.0"
  },
  "dependencies": {
    "rimraf": "^2.6.1"
  }
}
